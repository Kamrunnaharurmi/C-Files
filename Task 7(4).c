#include<stdio.h>
int main()
{
    int i,j,count,max,min;
    int A[]= {10,20,20,40,20}
    max=A[i],min=A[i];
    for(i=0; i<5; i++)
    {
        count=0;
        for(i=0; i<n; i++)
        {
            x[arr[i]]++;
        }
        printf("The frequency of all elements of this array :\n");
        for(i=0; i<100; i++)
        {
            if(max<A[j])
                max=A[j];
            {
                printf("%d occurs %d times\n",i,x[i]);
            }
        }
        return 0;
    }
